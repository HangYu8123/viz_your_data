# Skill impact — proposals to change viz_results

One atomic proposal per run at most. Status: proposed → accepted (user approved
and applied) → kept / reverted after the next run's feedback; or rejected.
